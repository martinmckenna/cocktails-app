import { Option as _Option } from './Select';

/* tslint:disable-next-line */
export interface Option<V = string | number, L = string | number>
  extends _Option<V, L> {}
export { default } from './Select';
